# Core dependencies
requests>=2.31.0
dnspython>=2.4.0
jinja2>=3.1.0
colorama>=0.4.6
tqdm>=4.66.0

# Optional web enumeration
httpx>=0.25.0
aiohttp>=3.9.0

# Screenshot capture (optional)
playwright>=1.40.0
Pillow>=10.0.0

# Charting/visualization (optional)
matplotlib>=3.8.0
plotly>=5.18.0
kaleido>=0.2.1
pandas>=2.1.0

# Note: External tools (optional, enhance performance):
# - gobuster: High-performance directory enumeration (brew/apt install gobuster)
# - dnsenum: Advanced DNS enumeration (apt install dnsenum, not on macOS brew)
# - dirb: Alternative directory scanner (apt install dirb, not on macOS brew)
# - searchsploit: Exploit database search (brew/apt install exploitdb)
# Python fallbacks are used when these tools are unavailable

